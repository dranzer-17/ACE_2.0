# ---------------------------------
# PostgreSQL Database Configuration for ACE_2.0
# ---------------------------------
# These credentials are used by the docker-compose 'db' service
DATABASE_USERNAME="postgres"
DATABASE_PASSWORD="ace_pass123"
DATABASE_NAME="ace_db"

# ---------------------------------
# Connection settings for your LOCAL backend app
# ---------------------------------
# We map port 5431 on your computer (localhost) to port 5432 inside the container
DATABASE_HOST="localhost"
DATABASE_PORT="5431"

# Full connection string your backend application will use
DATABASE_URL="postgresql://${DATABASE_USERNAME}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}"

# Add any other backend-specific environment variables for ACE_2.0 below
# e.g., API_KEY=your_secret_api_key